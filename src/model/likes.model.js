// const knex = require("../knex");

// const likesModel = {
//   select: async () => {
//     return knex("cards_likes").select("*");
//   },
//   showByUserId: async (user_id) => {
//     return knex("cards_likes").select("*").where({ user_id }).first();
//   },
//   showByCardId: async (card_id) => {
//     return knex("cards_likes").select("*").where({ card_id }).first();
//   }
// };
// module.exports = likesModel;
